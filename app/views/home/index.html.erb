<h1>Home</h1>

<%= Current.user %>

<%= 
  button_to "Sign Out", session_path, method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, 
  class: "block px-4 py-2 rounded bg-red-600 text-white" 
%>